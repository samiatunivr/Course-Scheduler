{
    "name": "university/course-scheduler",
    "description": "Enterprise AI-Powered University Course Scheduler & Faculty Workload Management System",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.2",
        "ext-pdo": "*",
        "ext-json": "*",
        "ext-mbstring": "*"
    },
    "suggest": {
        "phpoffice/phpspreadsheet": "Native .xlsx export (falls back to Excel-compatible XML/CSV without it)",
        "dompdf/dompdf": "Rich PDF rendering (falls back to built-in print-ready HTML/PDF generator without it)",
        "firebase/php-jwt": "JWT tokens for stateless API auth (falls back to built-in HMAC tokens)"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "scripts": {
        "serve": "php -S 0.0.0.0:8080 -t ../public_html ../public_html/router.php",
        "migrate": "php bin/migrate.php",
        "seed": "php bin/migrate.php --seed"
    }
}
